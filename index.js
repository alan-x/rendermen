const render = require('./src/renderermen').render
const tokenlize = require('./src/tokenlize').tokenlize
const compile = require('./src/compile').compile

module.exports = {
    render, tokenlize, compile
}